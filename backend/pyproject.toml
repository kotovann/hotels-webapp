[project]
name = "hotels-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django>=4.2",
    "djangorestframework",
    "psycopg2-binary",
    "python-decouple",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-django",
    "ruff",
]
